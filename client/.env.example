# Base URL of the Django REST API (no trailing slash).
# Local Django dev server default:
VITE_API_URL=http://localhost:8000

# Environment label: development | production | test
VITE_ENV=development
